import 'package:flutter_test/flutter_test.dart';

void main() {
  group(
    'UniqueID -',
    () {
      test(
        'Should be valid on .generate() constructor',
        () {},
      );
      test(
        'Should match input from right() on valid UID input',
        () {},
      );
      test(
        'Should match ValueFailure.empty from left() on empty input',
        () {},
      );
    },
  );
}
