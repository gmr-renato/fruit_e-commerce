import 'package:flutter_test/flutter_test.dart';

void main() {
  group(
    'validateStringNotEmpty',
    () {
      // validateStringNotEmpty()
      // Should return a right(ValueFailure.empty) if the input is empty.
      // Should return left(input) if the input is not empty.
    },
  );
}
